# Runtime requirements for local-first production usage.
# Development and documentation dependencies are in requirements-dev.txt.

# Core Web Framework
streamlit>=1.60.0,<1.61
streamlit-option-menu>=0.3.6,<0.5

# Image Processing & Computer Vision
Pillow>=12.0.0,<13
# Docling/RapidOCR requires opencv-python while the active EasyOCR fallback
# requires opencv-python-headless. Keep both on the same minor until the
# Docling migration removes this upstream namespace conflict.
opencv-python>=4.13.0,<5
opencv-python-headless>=4.13.0,<5
easyocr==1.7.2
numpy>=2.2.6,<2.6
scipy>=1.16.3,<1.19

# Machine Learning & Data Science
scikit-learn>=1.7.2,<1.10
pandas>=2.3.3,<3
# 23.0.1 contains the security fix missing from the old 21.0.0 baseline;
# 24.0.0 emitted an intermittent Windows access violation during pytest.
pyarrow==23.0.1
matplotlib>=3.10.7,<4
seaborn>=0.13.2,<0.14
# CUDA 12.4 wheels are installed first via requirements-native-cu124.txt.
torch==2.6.0
sentence-transformers>=5.6.1,<6
transformers>=5.5.0,<6
rank-bm25>=0.2.2,<0.3

# Graph Visualization (100% local, DSGVO-compliant)
graphviz>=0.21,<0.22
networkx>=3.6.1,<4
plotly>=6.9.0,<7

# OCR & Text Processing
# Document Understanding / local Docling converter
docling==2.94.0
PyMuPDF>=1.26.6,<2
pymupdf4llm>=0.2.6,<0.3
pdfminer.six>=20251107
pdfplumber>=0.11.8,<0.12
pypdf==6.14.2
# Local LLM stack
# 0.3.35: erste Version mit qwen35-Unterstützung (verifiziert 2026-08-27:
# "qwen35" + "gemma4" in lib/llama.dll, ggml-cuda.dll vorhanden, 2 GPUs erkannt).
# Ryzen 9 5950X (Zen 3) unterstützt kein AVX-512. Das offizielle cu124-Wheel
# meldet AVX512=1 und crasht beim Kontext-Init mit Windows 0xc000001d.
# 0.3.20 hatte kein qwen35 → Qwen3.8-Loads: "unknown model architecture".
# Upgrade/Neu-Build (CUDA, RTX 4090 sm_89) — PowerShell:
# $env:CMAKE_ARGS="-DGGML_CUDA=ON;-DGGML_AVX512=OFF;-DGGML_AVX512_VBMI=OFF;-DGGML_AVX512_VNNI=OFF;-DCMAKE_CUDA_ARCHITECTURES=89"
# pip install llama-cpp-python==0.3.35 --no-cache-dir --force-reinstall --no-binary :all:
llama-cpp-python==0.3.35
# Optional local vector index back-end
faiss-cpu>=1.13.0,<1.15
# Agent framework components used by psychological workflow modules
langchain-core>=1.2.14,<2
langgraph>=1.0.9,<2
pydantic>=2.12.4,<3

# Networking & Web
requests>=2.32.5,<3
urllib3>=2.5.0,<3
# Privacy-first web search (DuckDuckGo, renamed from duckduckgo_search)
ddgs>=9.9.3,<10
# Maintained local extraction strategies used by content_extractor/
trafilatura>=2.2.0,<3
readability-lxml>=0.8.4.1,<0.9
beautifulsoup4>=4.14.2,<5

# Data Formats
json5>=0.9.0
pyyaml>=6.0.2

# Async & Performance
aiohttp>=3.13.2,<4
aiosqlite>=0.22.1,<1
tenacity>=9.1.2,<10

# Utilities
psutil>=7.1.3,<8
python-dateutil>=2.9.0
pytz>=2025.2
tqdm>=4.66.0
click>=8.1.0
watchdog>=6.0.0,<7
# NVIDIA NVML-Bindings (VRAM-Monitoring, utils/gpu_devices.py, utils/vram_monitor.py).
# Offizieller Nachfolger des deprecated `pynvml`-Pakets (2026-09-04: der pynvml-Shim
# feuerte bei jedem `import pynvml*` — u. a. aus torch — eine FutureWarning;
# nvidia-ml-py liefert dasselbe `pynvml`-Modul ohne Warnung und ohne Redirector).
nvidia-ml-py>=13.610

# Observability (optional, used by VRAM telemetry export)
prometheus-client>=0.22.0
opentelemetry-api>=1.37.0
opentelemetry-sdk>=1.37.0

# Security & Encryption
cryptography>=45.0.0
